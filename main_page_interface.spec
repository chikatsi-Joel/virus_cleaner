# -*- mode: python ; coding: utf-8 -*-


a = Analysis(
    ['main_page_interface.py'],
    pathex=['.'],
    binaries=[],
    datas=[
        ('interface', 'interface'),
        ('interface/Images', 'interface/Images'),
        ('backbone', 'backbone')
    ],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.datas,
    [],
    name='CleanVirus',
    icon = 'icon.ico',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=False,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
)
